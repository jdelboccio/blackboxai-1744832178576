import SwiftUI

struct TeamView: View {
    var body: some View {
        VStack {
            Text("Meet Our Team")
                .font(.largeTitle)
                .padding()
            Text("Team member details will be displayed here.")
                .padding()
        }
    }
}
