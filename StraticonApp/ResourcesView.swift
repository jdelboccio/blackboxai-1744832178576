import SwiftUI

struct ResourcesView: View {
    var body: some View {
        VStack {
            Text("Company Resources")
                .font(.largeTitle)
                .padding()
            Text("Access to important documents and resources will be provided here.")
                .padding()
        }
    }
}
