import SwiftUI

struct ProjectsView: View {
    var body: some View {
        VStack {
            Text("Ongoing and Completed Projects")
                .font(.largeTitle)
                .padding()
            Text("Project details will be displayed here.")
                .padding()
        }
    }
}
