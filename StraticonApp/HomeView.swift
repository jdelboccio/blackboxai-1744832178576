import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Welcome to Straticon!")
                .font(.largeTitle)
                .padding()
            Text("Company news and updates will be displayed here.")
                .padding()
        }
    }
}
