import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Settings")
                .font(.largeTitle)
                .padding()
            Text("User preferences and app settings will be managed here.")
                .padding()
        }
    }
}
